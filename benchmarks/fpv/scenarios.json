{
  "_comment": "Machine-readable companion to scenarios.md, consumed by benchmarks/run_scenarios.sh. Each scenario grades two token classes separately: 'concept' (general methodology a capable model may know on its own) and 'exact' (tool-specific JasperGold command/flag that the model usually cannot derive — this is where distillation loss actually bites). type: control = knowledge file covers it; loss-probe = targets content the pipeline dropped.",
  "scenarios": [
    {
      "id": "counter-abstraction-timeout",
      "type": "control",
      "module": "complexity-management",
      "prompt": "My JasperGold proof is too deep because the DUT has a 32-bit timeout counter that counts to a large value before interesting behavior. How do I abstract it?",
      "expect_concept": ["counter abstract", "milestone"],
      "expect_exact": ["abstract -counter"]
    },
    {
      "id": "proof-decomposition-ag",
      "type": "control",
      "module": "complexity-management",
      "prompt": "A single assertion in JasperGold won't converge. I need to break the proof into smaller pieces and prove them separately. How does assume-guarantee work and how do I keep it sound?",
      "expect_concept": ["assume.?guarantee", "helper", "sound"],
      "expect_exact": []
    },
    {
      "id": "compact-global-helper",
      "type": "control",
      "module": "complexity-management",
      "prompt": "After a sane direct JasperGold prove, 412 generated no-duplicate assertions remain undetermined with no counterexample. I can state one global uniqueness invariant that may summarize the missing fact. How should I choose between a helper assertion and CAG, what gates make the result sound, and what are the exact key JasperGold Tcl commands for activating and using the helper?",
      "expect_concept": ["helper", "proven", "CAG", "(same|identical) (RTL|setup)|no assumptions solely|without (new|extra) assumptions"],
      "expect_exact": ["assert -set_helper", "with_helpers"]
    },
    {
      "id": "distributed-peer-cag",
      "type": "control",
      "module": "complexity-management",
      "prompt": "A JasperGold proof has hundreds of symmetric peer no-duplicate obligations. A compact helper was tried in isolation but remains undetermined and has essentially the same cone as the targets. What proof shape should I use next, which result is valid for signoff, and what is the exact key JasperGold Tcl command that creates this decomposition?",
      "expect_concept": ["CAG|compositional assume.?guarantee", "ROOT", "propagat"],
      "expect_exact": ["proof_structure", "compositional_assume_guarantee"]
    },
    {
      "id": "config-logic-cutpoint",
      "type": "loss-probe",
      "module": "complexity-management",
      "prompt": "In JasperGold my proof blows up because the design's configuration logic dominates the cone of influence. The configuration is written once at init and then held stable. How do I cut this complexity without producing false failures?",
      "expect_concept": ["cutpoint", "legal", "assum"],
      "expect_exact": ["setup_ndc"]
    },
    {
      "id": "per-property-simplification",
      "type": "loss-probe",
      "module": "complexity-management",
      "prompt": "One specific property in my JasperGold task is far harder than the rest and is dragging the whole run down. Is there a way to apply heavier simplification just to that property without changing the others?",
      "expect_concept": ["per.?property", "simplif"],
      "expect_exact": ["set_per_property_simplification"]
    },
    {
      "id": "mem-abstraction-stall",
      "type": "control",
      "module": "complexity-management",
      "prompt": "In JasperGold my prove -all proves the top controller assertion and covers every cover, but one embedded assertion is stuck: it is an arbitrary symbolic-address write-then-read property over a real 512x32 memory array, and it stays undetermined — multiple engines hit their per-property time limits with no counterexample. The RTL must stay unchanged. What should I do, and how do I report the result honestly?",
      "expect_concept": ["memory abstract", "symbolic|single.?slot|one.?(word|slot)|slot", "disclos|trusted|contract"],
      "expect_exact": ["bbox_i"]
    },
    {
      "id": "dbh-stalled-bound",
      "type": "control",
      "module": "engine-tuning",
      "prompt": "A meaningful JasperGold prove is still undetermined at a finite bound. I need to search deeper for bugs now, but I must not misreport the result as signoff. Which DBH modes should I start with, what Tcl shape configures them, and what does a no-hit run mean?",
      "expect_concept": ["cycle[_ ]swarm", "bound[_ ]swarm", "(?:hunt[ -])?auto", "non.?exhaustive|not.*(proof|signoff)|no.?hit.*not|neither.*(proof|signoff)"],
      "expect_exact": ["hunt -config", "hunt -run", "first_trace_attempt", "max_trace_length"]
    },
    {
      "id": "dbh-known-bug-reproduction",
      "type": "control",
      "module": "engine-tuning",
      "prompt": "I have an external failing trace for a JasperGold target. I want to reproduce the bug, preserve useful steering states, then challenge the RTL fix with diverse paths. What DBH flow and exact commands should I use?",
      "expect_concept": ["qualif|confirm", "helper", "divers", "not.*proof|search confidence"],
      "expect_exact": ["visualize -confirm", "set_store_trace unlimited", "hunt -run -auto", "auto_helper_num", "force"]
    },
    {
      "id": "dbh-regression-coverage",
      "type": "control",
      "module": "engine-tuning",
      "prompt": "In a JasperGold regression, previously proven properties are now slow, an old CEX has not reappeared, and several critical coverage items remain uncovered. How should I carry history into DBH and close reachable coverage without claiming unreachability? Include the exact key Tcl commands.",
      "expect_concept": ["previous|prior|history|old", "cycle[_ ]swarm", "bound[_ ]swarm", "coverage|cover", "not.*unreachable|cannot.*unreachable|unreachable.*only.*exhaustive|only.*exhaustive.*unreachable"],
      "expect_exact": ["report -csv", "check_cov -create_cover_item_property", "check_cov -measure"]
    }
  ]
}
