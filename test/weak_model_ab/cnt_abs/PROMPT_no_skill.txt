You are solving a JasperGold formal-verification benchmark.
Work ONLY in /home/robin/Projects/awesome-formal-verification-skill/test/weak_model_ab/cnt_abs
and write ALL scripts/logs/reports ONLY under that directory's no_skill/ subfolder.
First read ./README.md and ./cnt_abstract_eg.sv in that directory.

Constraints:
- Do NOT use any formal-verification skill or external knowledge base.
- Do NOT read /home/robin/Projects/awesome-formal-verification-skill/knowledge,
  /adapters, /tool-specific, /raw-docs, /extractions, or any other testcase or prior report.
- Use ONLY your own general JasperGold knowledge.
- Do NOT edit cnt_abstract_eg.sv.
- Every JasperGold project directory MUST live under no_skill/ : pass
  `-proj no_skill/proj_<timestamp>` on EVERY jg call. Never run a bare
  `jg -batch` (it creates ./jgproject at the case root, outside your arm dir).

Task: verify the design as described in README.md. Write the SVA properties that
capture the intended behaviour of outputs A, B, C, D, prove them with batch
JasperGold (jg -batch, timestamped -proj dirs), and demonstrate the asserting
conditions are reachable (non-vacuity). Iterate until you reach a full proof or
can clearly show you cannot.

When finished, write no_skill/report.md containing: the properties you wrote,
the exact jg commands, final proven/cex/undetermined counts, cover results,
whether the result is an unqualified raw-RTL proof or relies on any
assumption/abstraction (disclose it explicitly), the wall-clock time from start
to first full proof, and paths to the jg logs.
